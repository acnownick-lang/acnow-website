# Netlify configuration file for A/C Now LLC
# Configures build settings, redirects, headers, serverless functions, and edge functions.

[build]
  publish = "."

[functions]
  node_bundler = "esbuild"

# Edge functions disabled locally to bypass Deno CLI packaging errors.
# They will be restored automatically on dev server exit.
# [[edge_functions]]
#   path = "/"
#   function = "localize-geo"
